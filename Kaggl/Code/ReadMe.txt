# To execute the code run following command
python <model_name>.py

model_name : simple, G4, VGG, ResNet

if you have multiple python version then :
python2.7 <model_name>.py


# Make sure you have around 5GB space because running the code will create h5py files which take some space.
# The code currently runs on only 10 percent data. It works on 100 percent data also.

